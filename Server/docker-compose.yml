version: '3.9'
services:
  auth-api:
    build:
      context: .
      target: dev
    ports:
      - "3000:3000"
    environment:
      PORT: 3000
      JWT_SECRET: change-me
      DB_PATH: /data/app.db
    volumes:
      - .:/app
      - ./data:/data
