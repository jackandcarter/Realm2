window.REALM_PORTAL_CONFIG = {
  /**
   * Base URL for the Realm2 authentication service.
   * Update this to point at your VPS or hosted API when deploying.
   */
  API_BASE_URL: 'http://localhost:3000',
};
